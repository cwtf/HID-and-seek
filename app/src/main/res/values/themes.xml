<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The real theming happens in Compose (MaterialExpressiveTheme). This
        exists only so the window has sane colours before Compose draws, and so
        no framework action bar appears above the Compose content.
    -->
    <style name="Theme.HidAndSeek" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>
</resources>
